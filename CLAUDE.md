# mr-t

mr-t exists to accelerate results.

Friction points are recorded via "mrt complaints add".
Over time we build and refine cli cmds to address these friction points and to build leverage.

Structure: the src/commands dir follows the same exact nesting as the cli with exactly one command per file. (exluding mod.rs files ofc).
